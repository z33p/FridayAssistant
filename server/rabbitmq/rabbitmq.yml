apiVersion: rabbitmq.com/v1beta1
kind: RabbitmqCluster
metadata:
  name: friday-rabbitmq
spec:
  replicas: 1
  resources:
    requests:
      cpu: "20m"
      memory: "144Mi"
    limits:
      cpu: "150m"
      memory: "256Mi"